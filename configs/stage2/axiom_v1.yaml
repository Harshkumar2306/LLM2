base: ../base.yaml

# 32M Parameter Scale
d_model: 512
n_layers: 8
n_heads: 8
max_iters: 25000

# Final architecture components (TBD based on Stage 1 results)
# For now, placeholder for best components
ffn_type: standard
position_type: absolute
