base: ../base.yaml

# Phase 2: 100M Parameter Scale, 100K Steps
d_model: 768
n_layers: 12
n_heads: 12
max_iters: 100000
lr_decay_iters: 100000

# To be filled with the settings of the winning architecture from Phase 1B
experiment_name: "axiom-foundation-v1.0"
